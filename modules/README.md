# Modules

`modules/` 保存已經真實專案驗證、可以搬到其他 Repository 的實作索引。

正式模組應包含：

- 安裝方式。
- 相依套件。
- 環境變數。
- 使用範例。
- 測試方式。
- 適用範圍。
- 限制。

第一階段只建立文件骨架，不假裝模組已經完成。未經驗證的做法請先放在 `knowledge/` 或任務結果中的 Candidate for reuse。
